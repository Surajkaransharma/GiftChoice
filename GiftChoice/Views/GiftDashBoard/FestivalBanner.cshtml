
@{
    ViewBag.Title = "FestivalBanner";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<style>
    img {
        width: 50px
    }

    .drop-zone {
        width: 300px;
        height: 200px;
        border: 2px dashed #ccc;
        margin: 0 auto;
        text-align: center;
        padding: 20px;
        margin-bottom: 20px;
    }

        .drop-zone p {
            margin-bottom: 10px;
        }

    .image-input {
        display: none;
    }

    .preview {
        width: 150px;
        margin-bottom: 10px;
        display: none;
        margin: 0 auto;
    }

    .remove-image {
        background-color: #ccc;
        border: none;
        padding: 5px 10px;
        cursor: pointer;
        opacity: 0.5;
        transition: opacity 0.2s ease;
    }

        .remove-image:hover {
            opacity: 1;
        }
</style>
<script src="~/Angularjs/Admin.js"></script>
<div class="page-wrapper" ng-app="AdminApp" ng-controller="AdminController">
    <div class="page-content">
        <div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
            <div class="breadcrumb-title pe-3">Adversities Banner</div>
            <div class="ps-3">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb mb-0 p-0">
                        <li class="breadcrumb-item">
                            <a href="javascript:;"><i class="bx bx-home-alt"></i></a>
                        </li>
                        <li class="breadcrumb-item active" aria-current="page">Adversities Banner Form</li>
                    </ol>
                </nav>
            </div>

        </div>
        <div class="row">
            <div class="col-xl-4">

                <div class="card">
                    <div class="card-body p-4">

                        <form class="row g-3" enctype="multipart/form-data">

                            @*<div class="col-md-12">
                                <label for="input1" class="form-label">Adversities Banner Title</label>
                                <input type="text" class="form-control" id="FBTitle" ng-model="FestivalBanner.FBTitle" placeholder="Festival Banner Title">

                            </div>*@
                            <div class="col-md-12">
                                <label for="input1" class="form-label">Festival Banner Url</label>
                                <input type="text" class="form-control" id="FBUrl" ng-model="FestivalBanner.FBUrl" placeholder="Festival Banner Url">

                            </div>
                            <div class="col-md-12">

                                <label for="inputFirstName" class="form-label">Image <span style="color:#ff0000;font-weight:bolder;"></span></label>
                                <p>Drop Zone 1: Drag & drop your image here or click to browse</p>
                                <div class="drop-zone">
                                    <input type="file" class="image-input" accept="image/*" upload-file="FestivalBanner.Image">
                                    <img class="preview" src="" alt="Image Preview">
                                </div>
                                <button class="remove-image" disabled>Remove Image</button>
                                @*<section id="image-section">
                                        <div id="uploadArea" class="upload-area">
                                            <!-- Header -->
                                            <div class="upload-area__header" style="display:none">
                                                <h1 class="upload-area__title">Upload your file</h1>
                                                <p class="upload-area__paragraph">
                                                    File should be an image
                                                    <strong class="upload-area__tooltip">
                                                        Like
                                                        <span class="upload-area__tooltip-data"></span>
                                                    </strong>
                                                </p>
                                            </div>
                                            <!-- End Header -->
                                            <!-- Drop Zoon -->
                                            <div id="dropZoon" class="upload-area__drop-zoon drop-zoon">
                                                <span class="drop-zoon__icon">
                                                    <i class='bx bxs-file-image'></i>
                                                </span>
                                                <p class="drop-zoon__paragraph">Drop your file here or Click to browse</p>
                                                <span id="loadingText" class="drop-zoon__loading-text">Please Wait</span>
                                                <img src="" alt="Preview Image" id="previewImage" class="drop-zoon__preview-image" draggable="false">
                                                <input type="file" id="fileInput" upload-file="FestivalBanner.Image" class="drop-zoon__file-input" multiple>
                                            </div>
                                            <!-- End Drop Zoon -->
                                            <!-- File Details -->
                                            <div id="fileDetails" class="upload-area__file-details file-details" style="display: none">
                                                <h3 class="file-details__title">Uploaded File</h3>

                                                <div id="uploadedFile" class="uploaded-file">
                                                    <div class="uploaded-file__icon-container">
                                                        <i class='bx bxs-file-blank uploaded-file__icon'></i>
                                                        <span class="uploaded-file__icon-text"></span>

                                                    </div>

                                                    <div id="uploadedFileInfo" class="uploaded-file__info">
                                                        <span class="uploaded-file__name">Proejct 1</span>
                                                        <span class="uploaded-file__counter">0%</span>
                                                    </div>
                                                </div>
                                            </div>
                                            <!-- End File Details -->
                                        </div>
                                    </section>*@

                            </div>




                            <div class="col-md-12">
                                <div class="d-md-flex d-grid align-items-center gap-3">
                                    <button type="button" id="btn" ng-click="SubmitFestivalBanner()" class="btn btn-primary px-4">Submit</button>
                                    <button type="button" id="edbtn" ng-click="UpdateFestivalBanner()" style="display: none" class="btn btn-primary px-4">Update</button>
                                    <button type="button" class="btn btn-light px-4" onClick="refreshPage()">Reset</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>

            </div>
            <div class="col-xl-8">
                <div class="card">
                    <div class="card-body">
                        <div class="d-lg-flex align-items-center mb-4 gap-3">



                        </div>
                        <div class="table-responsive">
                            <table class="table mb-0 table-bordered" ng-init="GetFestivalBanner()">
                                <thead class="table-light">
                                    <tr>
                                        <th>S.No.</th>
                                        @*<th>Festival Banner Title</th>*@
                                        <th>Adversities Banner Url</th>


                                        <th>Image</th>
                                        <th>Active</th>

                                        <th>Action</th>

                                    </tr>
                                </thead>
                                <tbody>
                                    <tr ng-repeat="m in GetFestivalBannerData">
                                        <td>{{$index+1}}</td>
                                        @*<td>{{m.FBTitle}}</td>*@
                                        <td>{{m.FBUrl}}</td>

                                        <td><img src="~/images/FestivalBanner/{{m.FBImage}}"></td>
                                        <td><a ng-click="FestivalBannerActiveDeActive(m.FBannerId)"> <span class="badge {{m.Active==1? 'bg-success' : 'bg-danger'}}">{{m.Active==1?'Active':'Deactive'}}</span></a> </td>
                                        <td><a><i class="bx bxs-pencil" ng-click="GetFestivalBannerByid($index)"></i></a></td>
                                    </tr>


                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    const dropZones = document.querySelectorAll('.drop-zone');
    const imageInputs = document.querySelectorAll('.image-input');
    const previews = document.querySelectorAll('.preview');
    const removeButtons = document.querySelectorAll('.remove-image');

    dropZones.forEach((dropZone, index) => {
        dropZone.addEventListener('dragover', (event) => {
            event.preventDefault();
            dropZone.classList.add('dragover');
        });

        dropZone.addEventListener('dragleave', () => {
            dropZone.classList.remove('dragover');
        });

        dropZone.addEventListener('drop', (event) => {
            event.preventDefault();
            dropZone.classList.remove('dragover');

            const file = event.dataTransfer.files[0];
            if (!file || !file.type.startsWith('image/')) {
                alert('Please drag and drop an image file.');
                return;
            }
            uploadFile(file, previews[index], removeButtons[index], imageInputs[index]);
        });

        dropZone.addEventListener('click', () => {
            imageInputs[index].click();
        });

        imageInputs[index].addEventListener('change', (event) => {
            const file = event.target.files[0];
            uploadFile(file, previews[index], removeButtons[index], imageInputs[index]);
        });

        removeButtons[index].addEventListener('click', () => {
            previews[index].src = '';
            imageInputs[index].value = '';
            previews[index].style.display = 'none';
            removeButtons[index].disabled = true;
        });
    });

    function uploadFile(file, preview, removeButton, input) {
        const reader = new FileReader();
        reader.onload = (e) => {
            preview.src = e.target.result;
            preview.style.display = 'block';

            removeButton.disabled = false;
        };
        reader.readAsDataURL(file);
        debugger
        input.files = event.dataTransfer.files;
    }
</script>
